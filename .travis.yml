language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-asciidoc' ]; then cd .. && eval "mv $currentfolder generator-asciidoc" && cd generator-asciidoc; fi

